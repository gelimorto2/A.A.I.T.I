// Minimal ambient declaration to satisfy TypeScript in environments
// where the editor can't resolve node_modules types reliably.
// The actual package ships with typings, so this is a safe fallback.
declare module 'lightweight-charts';
